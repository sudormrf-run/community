
---
import Layout from '../../layouts/Layout.astro';

const pastEvents = [
  {
    title: "HuggingFace LeRobot Hackathon",
    date: "2024년 6월 22일",
    description: "HuggingFace와 함께한 LeRobot 해커톤. 로봇을 직접 만들고 프로그래밍하며 AI의 물리적 구현을 경험했습니다.",
    image: "/images/events/lerobot-hackathon.jpg",
    link: "https://sudormrf.run/2025/06/24/huggingface-lerobot-hackathon-review/",
    youtubeId: "HgN0qIFSd8I"
  }
];

const upcomingEvents = [
  {
    title: "Physical AI Workshop",
    date: "2025년 8월 14일",
    description: "로보티즈와 함께하는 Physical AI 워크샵. 참가신청은 마감되었으며, 유튜브 라이브로 시청 가능합니다.",
    image: "/community/events/2025/physical-ai-workshop-poster.png",
    link: "/community/events/2025/physical-ai-workshop",
    youtubeLink: "https://www.youtube.com/live/Vqk7rdlqSdE"
  }
];
---

<Layout title="이벤트">
  <div class="events-container">
    <h1>이벤트</h1>
    <p class="subtitle">Sudoremove의 최신 소식과 이벤트</p>

    <div class="notice-section">
      <div class="notice-card">
        <span class="notice-badge">📢 공지</span>
        <h3>LeRobot 커뮤니티에 참여하세요!</h3>
        <p>카카오톡 오픈채팅방에서 LeRobot 프로젝트 관련 소식과 정보를 공유합니다.</p>
        <a href="https://open.kakao.com/o/gDyJjDIg" target="_blank" rel="noopener noreferrer" class="kakao-link">
          💬 카카오톡 LeRobot 오픈채팅방 참여하기
        </a>
      </div>
    </div>

    <h2>지난 행사</h2>
    <div class="events-grid">
      {pastEvents.map(event => (
        <div class="event-card-wrapper">
          <a href={event.link} class="event-card-link" target="_blank" rel="noopener noreferrer">
            <div class="event-card past">
              <img src={event.image} alt={event.title} />
              <div class="event-info">
                <h3>{event.title}</h3>
                <p class="event-date-text">{event.date}</p>
                <p>{event.description}</p>
              </div>
            </div>
          </a>
          <div class="video-container">
            <iframe 
              src={`https://www.youtube.com/embed/${event.youtubeId}`}
              frameborder="0" 
              allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
              allowfullscreen>
            </iframe>
          </div>
        </div>
      ))}
    </div>

    <h2>예정된 행사</h2>
    <div class="events-grid">
      {upcomingEvents.map(event => (
        <div class="event-card-wrapper">
          <a href={event.link} class="event-card-link">
            <div class="event-card upcoming">
              <img src={event.image} alt={event.title} />
              <div class="event-info">
                <h3>{event.title}</h3>
                <p class="event-date-text">{event.date}</p>
                <p>{event.description}</p>
                {event.youtubeLink && (
                  <a href={event.youtubeLink} target="_blank" rel="noopener noreferrer" class="youtube-link">
                    🔴 유튜브 라이브 시청하기
                  </a>
                )}
              </div>
            </div>
          </a>
        </div>
      ))}
    </div>
  </div>
</Layout>

<style>
  .events-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
    color: #ffffff;
  }

  h1, h2 {
    color: #fd7318;
  }

  h1 {
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
  }
  
  h2 {
    margin-top: 3rem;
    margin-bottom: 1.5rem;
  }

  .subtitle {
    font-size: 1.2rem;
    color: #b8b8b8;
    margin-bottom: 3rem;
  }

  .events-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
    gap: 2rem;
  }

  .event-card-link {
    text-decoration: none;
    color: inherit;
  }

  .event-card {
    background: #251730;
    border-radius: 0.5rem;
    overflow: hidden;
    transition: transform 0.3s, box-shadow 0.3s;
    height: 100%;
  }

  .event-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
  }

  .event-card img {
    width: 100%;
    height: 200px;
    object-fit: cover;
    background: #333;
  }

  .event-card .event-info {
    padding: 1.5rem;
  }

  .event-card h3 {
    margin-bottom: 0.5rem;
    color: #fd7318;
  }

  .event-date-text {
    color: #43d6a8;
    font-size: 0.9rem;
    margin-bottom: 1rem;
  }

  .event-card p {
    color: #b8b8b8;
  }

  .event-card.upcoming .event-info h3 {
    color: #5fadfe;
  }
  
  .video-container {
    position: relative;
    padding-bottom: 56.25%; /* 16:9 */
    height: 0;
    overflow: hidden;
    max-width: 100%;
    background: #000;
    margin-top: 1rem;
    border-radius: 0.5rem;
  }

  .video-container iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
  }

  .youtube-link {
    display: inline-block;
    background: #ff0000;
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 0.25rem;
    text-decoration: none;
    font-weight: bold;
    margin-top: 1rem;
    transition: all 0.3s;
  }

  .youtube-link:hover {
    background: #cc0000;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(255, 0, 0, 0.4);
  }

  .notice-section {
    margin: 2rem 0 3rem 0;
  }

  .notice-card {
    background: linear-gradient(135deg, rgba(253, 115, 24, 0.1), rgba(67, 214, 168, 0.1));
    border: 2px solid #fd7318;
    border-radius: 1rem;
    padding: 2rem;
    position: relative;
    overflow: hidden;
  }

  .notice-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, #fd7318, #ffc107, #43d6a8);
    animation: shimmer 3s ease-in-out infinite;
  }

  @keyframes shimmer {
    0%, 100% { transform: translateX(-100%); }
    50% { transform: translateX(100%); }
  }

  .notice-badge {
    display: inline-block;
    background: #fd7318;
    color: #0d030f;
    padding: 0.25rem 0.75rem;
    border-radius: 0.5rem;
    font-size: 0.9rem;
    font-weight: bold;
    margin-bottom: 1rem;
  }

  .notice-card h3 {
    color: #43d6a8;
    margin-bottom: 0.75rem;
    font-size: 1.5rem;
  }

  .notice-card p {
    color: #ffffff;
    margin-bottom: 1.5rem;
    font-size: 1.1rem;
  }

  .kakao-link {
    display: inline-block;
    background: #fee500;
    color: #3c1e1e;
    padding: 0.75rem 1.5rem;
    border-radius: 0.5rem;
    text-decoration: none;
    font-weight: bold;
    transition: all 0.3s;
    font-size: 1rem;
  }

  .kakao-link:hover {
    background: #fdd835;
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(254, 229, 0, 0.4);
  }

  @media (max-width: 768px) {
    .notice-card {
      padding: 1.5rem;
    }

    .notice-card h3 {
      font-size: 1.25rem;
    }

    .notice-card p {
      font-size: 1rem;
    }

    .kakao-link {
      display: block;
      text-align: center;
    }
  }
</style>
