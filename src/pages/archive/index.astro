---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="행사 Archive">
  <div class="archive-container">
    <h1>행사 Archive</h1>
    <p class="subtitle">Sudoremove가 진행한 행사와 앞으로의 일정</p>

    <section class="upcoming-events">
      <h2>예정된 행사</h2>
      <div class="event-card upcoming">
        <div class="event-date">
          <span class="month">JAN</span>
          <span class="day">15</span>
          <span class="year">2025</span>
        </div>
        <div class="event-info">
          <h3>Physical AI 워크샵</h3>
          <p>HuggingFace LeRobot을 활용한 실습 워크샵</p>
          <div class="event-details">
            <span>📍 서울</span>
            <span>⏰ 14:00 - 18:00</span>
            <span>👥 30명</span>
          </div>
          <a href="#" class="btn-register">사전 등록</a>
        </div>
      </div>
    </section>

    

    <section class="past-events">
      <h2>지난 행사</h2>
      <div class="horizontal-scroll">
        <div class="events-grid">
          <a href="/events" class="event-card-link">
            <div class="event-card past">
              <img src="/images/events/lerobot-hackathon.jpg" alt="HuggingFace LeRobot Hackathon" />
              <div class="event-info">
                <h3>HuggingFace LeRobot Hackathon</h3>
                <p class="event-date-text">2024년 6월 22일</p>
              </div>
            </div>
          </a>
        </div>
      </div>
    </section>

    <section class="event-gallery">
      <h2>행사 갤러리</h2>
      <div class="horizontal-scroll">
        <div class="gallery-grid">
          <div class="gallery-item video-item">
            <iframe 
              src="https://www.youtube.com/embed/HgN0qIFSd8I"
              frameborder="0" 
              allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
              allowfullscreen>
            </iframe>
          </div>
        </div>
      </div>
    </section>
  </div>
</Layout>

<style>
  /* CSS 변수 정의 */
  :root {
    --primary-color: #1a0f1f;
    --secondary-color: #251730;
    --accent-color: #fd7318;
    --text-color: #ffffff;
    --text-muted: #b8b8b8;
    --color-green: #43d6a8;
    --color-blue: #5fadfe;
  }
  
  /* 배경색 강제 적용 */
  body {
    background: #0d030f !important;
    color: #ffffff !important;
  }
  
  .archive-container {
    max-width: 1200px;
    margin: 0 auto;
    color: var(--text-color);
  }

  h1 {
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
  }

  .subtitle {
    font-size: 1.2rem;
    color: var(--text-muted);
    margin-bottom: 3rem;
  }

  section {
    margin-bottom: 4rem;
  }

  section h2 {
    color: var(--accent-color);
    margin-bottom: 2rem;
  }

  /* 예정된 행사 */
  .event-card.upcoming {
    background: var(--secondary-color);
    border: 2px solid var(--accent-color);
    border-radius: 1rem;
    padding: 2rem;
    display: flex;
    gap: 2rem;
    align-items: center;
  }

  .event-date {
    text-align: center;
    padding: 1rem;
    background: var(--accent-color);
    border-radius: 0.5rem;
    min-width: 100px;
  }

  .event-date span {
    display: block;
    color: white;
  }

  .event-date .month {
    font-size: 0.9rem;
  }

  .event-date .day {
    font-size: 2rem;
    font-weight: bold;
    line-height: 1;
  }

  .event-date .year {
    font-size: 0.9rem;
  }

  .event-info {
    flex: 1;
  }

  .event-info h3 {
    margin-bottom: 0.5rem;
  }

  .event-details {
    display: flex;
    gap: 1.5rem;
    margin: 1rem 0;
    font-size: 0.9rem;
    color: #ccc;
  }

  .btn-register {
    display: inline-block;
    background: var(--accent-color);
    color: white;
    padding: 0.5rem 1.5rem;
    border-radius: 0.25rem;
    text-decoration: none;
    transition: transform 0.3s;
  }

  .btn-register:hover {
    transform: translateY(-2px);
  }

  /* 지난 행사 */
  .events-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
    gap: 2rem;
  }

  .event-card.past {
    background: var(--secondary-color);
    border-radius: 0.5rem;
    overflow: hidden;
    transition: transform 0.3s;
  }

  .event-card.past:hover {
    transform: translateY(-4px);
  }

  .event-card.past img {
    width: 100%;
    height: 200px;
    object-fit: cover;
    background: #333;
  }

  .event-card.past .event-info {
    padding: 1.5rem;
  }

  .horizontal-scroll {
    overflow-x: auto;
    white-space: nowrap;
    padding-bottom: 1rem;
  }

  .horizontal-scroll .events-grid,
  .horizontal-scroll .gallery-grid {
    display: inline-flex;
    gap: 1rem;
  }

  .event-card.past {
    width: 300px;
  }

  .gallery-item {
    width: 350px;
    height: 200px;
  }

  .video-item iframe {
    width: 100%;
    height: 100%;
  }

  .event-date-text {
    color: var(--accent-color);
    font-size: 0.9rem;
    margin-bottom: 0.5rem;
  }

  .event-stats {
    display: flex;
    gap: 1rem;
    margin-top: 1rem;
    font-size: 0.85rem;
    color: var(--text-muted);
  }

  /* 갤러리 */
  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 1rem;
  }

  .gallery-item {
    aspect-ratio: 1;
    overflow: hidden;
    border-radius: 0.5rem;
    background: #333;
  }

  .gallery-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s;
  }

  .gallery-item:hover img {
    transform: scale(1.1);
  }

  .video-container {
    position: relative;
    padding-bottom: 56.25%; /* 16:9 */
    height: 0;
    overflow: hidden;
    max-width: 100%;
    background: #000;
    margin-top: 1rem;
    border-radius: 0.5rem;
  }

  .video-container iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
  }

  @media (max-width: 768px) {
    .event-card.upcoming {
      flex-direction: column;
      text-align: center;
    }

    .event-details {
      justify-content: center;
    }
  }
</style>