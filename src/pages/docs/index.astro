---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="문서">
  <div class="docs-container">
    <h1>문서</h1>
    <p class="subtitle">Sudoremove의 AI 교육 자료와 튜토리얼</p>
    
    <div class="notice">
      <p>📚 새로운 문서 사이트가 준비되었습니다!</p>
      <a href="/community/docs/" class="btn btn-primary">새 문서 사이트로 이동</a>
    </div>
    
    <section class="docs-section">
      <h2>LeRobot</h2>
      <div class="docs-grid">
        <a href="/community/docs/physical-ai/lerobot/intro" class="doc-card">
          <h3>🤗 LeRobot 시작하기</h3>
          <p>HuggingFace LeRobot 완벽 가이드</p>
        </a>
      </div>
    </section>

    <section class="docs-section">
      <h2>LLM 강의 아카이브</h2>
      <div class="docs-grid">
        <div class="doc-card" style="opacity: 0.6; cursor: not-allowed;">
          <h3>LLM 기초</h3>
          <p>준비 중입니다</p>
        </div>
        <div class="doc-card" style="opacity: 0.6; cursor: not-allowed;">
          <h3>고급 주제</h3>
          <p>준비 중입니다</p>
        </div>
      </div>
    </section>
  </div>
</Layout>

<style>
  /* 배경색 강제 적용 */
  body {
    background: var(--color-background) !important;
    color: var(--color-text) !important;
  }
  
  
  .docs-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: var(--spacing-8) var(--spacing-6);
    color: var(--color-text);
  }

  h1 {
    font-size: var(--text-5xl);
    font-weight: var(--font-extrabold);
    margin-bottom: var(--spacing-3);
    background: linear-gradient(135deg, var(--color-text) 0%, var(--color-accent) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .subtitle {
    font-size: var(--text-xl);
    color: var(--color-text-muted);
    margin-bottom: var(--spacing-12);
    line-height: var(--leading-relaxed);
  }

  .docs-section {
    margin-bottom: var(--spacing-12);
  }

  .docs-section h2 {
    font-size: var(--text-3xl);
    font-weight: var(--font-bold);
    color: var(--color-text);
    margin-bottom: var(--spacing-6);
    display: flex;
    align-items: center;
    gap: var(--spacing-3);
  }
  
  .docs-section h2::before {
    content: '';
    width: 4px;
    height: 1.5em;
    background: var(--color-accent);
    border-radius: var(--radius-sm);
  }

  .docs-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: var(--spacing-6);
  }

  .doc-card {
    background: var(--color-surface);
    padding: var(--spacing-6);
    border-radius: var(--radius-lg);
    text-decoration: none;
    color: var(--color-text);
    transition: all var(--transition-base);
    border: 1px solid var(--color-border);
    position: relative;
    overflow: hidden;
  }
  
  .doc-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, var(--color-accent), var(--color-yellow));
    transform: translateX(-100%);
    transition: transform var(--transition-slow);
  }
  
  .doc-card:hover::before {
    transform: translateX(0);
  }

  .doc-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
    border-color: var(--color-accent);
    background: var(--color-surface-hover);
  }

  .doc-card h3 {
    font-size: var(--text-xl);
    font-weight: var(--font-semibold);
    color: var(--color-text);
    margin-bottom: var(--spacing-3);
    display: flex;
    align-items: center;
    gap: var(--spacing-2);
  }

  .doc-card p {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
  }


  .notice {
    background: linear-gradient(135deg, var(--color-accent-light) 0%, rgba(253, 115, 24, 0.05) 100%);
    border: 1px solid var(--color-accent);
    border-radius: var(--radius-lg);
    padding: var(--spacing-10);
    text-align: center;
    margin-bottom: var(--spacing-12);
    box-shadow: 0 0 40px rgba(253, 115, 24, 0.1);
    position: relative;
    overflow: hidden;
  }
  
  .notice::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(253, 115, 24, 0.1) 0%, transparent 70%);
    animation: pulse 4s ease-in-out infinite;
  }
  
  @keyframes pulse {
    0%, 100% { transform: scale(1); opacity: 0.5; }
    50% { transform: scale(1.1); opacity: 0.3; }
  }

  .notice p {
    font-size: var(--text-xl);
    font-weight: var(--font-medium);
    margin-bottom: var(--spacing-4);
    position: relative;
    z-index: 1;
  }
  
  .notice .btn {
    position: relative;
    z-index: 1;
  }
  
  /* 모바일 반응형 */
  @media (max-width: 768px) {
    .docs-container {
      padding: var(--spacing-6) var(--spacing-4);
    }
    
    h1 {
      font-size: var(--text-4xl);
    }
    
    .docs-grid {
      grid-template-columns: 1fr;
      gap: var(--spacing-4);
    }
  }
</style>